---
name: project_create_thread_remaining_assumes
description: create_thread_from_staged_page inv() rebuild — which assumes are proven vs the 3 that need new lemmas/spec changes
metadata: 
  node_type: memory
  type: project
  originSessionId: 11731d4e-6c71-43b7-b3d9-440871f93b17
---

`create_thread_from_staged_page` (src/kernel/implementation/syscall_new_thread.rs) re-establishes `inv()` after retype+container-set-update+owned_threads-push. Most conjuncts now PROVEN from reveals + old-invariant-in-scope + case-split on the fresh thread (the recipe: `assert(P(old(self)...))` — free from `old(self).inv()` — then split the forall on `t2 == page_ptr` vs not, bridging pre-existing entries via `self.X.spec_index(t2)==old(self).X.spec_index(t2)`). This includes container_thread_endpoint_wf and container_thread_scheduler_wf (the latter needs `container_thread_wf(old)` for a dom fact).

**container_process_allocator_quota_wf is now PROVEN** (2026-07-20). Built the fold-insert-of-zero axiom family in lemma_t/kernel_fold_axioms.rs: 6 external_body axioms `lemma_thread_{direct,indirect}_pending_{4k,2m,1g}_fold_insert_zero[_at_depth]` (twins of the `_fold_eq` family, requiring `s.contains(new_t)==false` + the new element's pending==0 + per-element eq elsewhere; ensures `s.insert(new_t).fold == s.fold`). Soundness = vstd `lemma_fold_insert` (which IS provable, but needs `s.finite()` — absent as an invariant on the ghost sets — plus fold-congruence-under-pointwise-eq, so the whole family stays external_body TCB, matching `_fold_eq`). Then a proven wrapper `container_process_allocator_quota_wf_preserved_on_thread_add(pre,post,dc,t_ptr,uppers)` in the syscall file (NOT kernel_preservation.rs — that's DISABLED in lemma_u/mod.rs, off-limits) dispatches per size: process-quota `_fold_eq`, direct-thread insert-zero-at-dc-else-eq, indirect-thread insert-zero-at-ancestor-else-eq, allocators byte-equal. 310k rlimit, closed on first try; trimmed to bare lemma dispatch (subset_of/dom-membership/contains-false primers all delete-and-reverify dead). Two enablers: (a) added `thread_value.free_quota_pending_clean()` precondition to create_thread_from_staged_page (fresh thread ⟹ pending 0); (b) added a "non-ancestors keep owned_indirect_threads" conjunct to `container_map_gained_thread` (its producer add_thread_to_container_sets already proves it — monotonic). Call site needs `reveal(container_uppertree_seq_wf)` (load-bearing: gives ancestor-depth for the pending-0 precondition).

**container_thread_wf is now PROVEN too (2026-07-20) — create_thread_from_staged_page is now ZERO-assume.** The "hard 4-quantifier container-set lemma" was a mirage: `container_thread_wf_preserved_on_thread_add`'s body is now a SINGLE `reveal(container_thread_wf)` — every case-split assert was delete-and-reverify dead. It was never hard to prove, only UNDER-SPECIFIED: two hypotheses were missing from its `requires`, so a from-scratch attempt failed at the conjuncts and looked like it needed grinding. The two: (a) `post_cm[c].view() == pre_cm[c].view()` (I'd only had view_rodata() equality; conjunct 1 reads cm[c].view().uppertree_seq, so untouched-container transport needs full view() eq — add_thread_to_container_sets proves it); (b) `uppers ⊆ pre_cm.dom()` (conjunct 4's fresh case needs each ancestor in-dom; caller has it as a precond). With those in requires, triggers fire on their own. The only real work was the CALL SITE, where the trap was a WRONG REVEAL: the `uppertree_seq.view().len() == depth` fact lives in `container_tree_fields_wf` (part of container_perms_wf), NOT `container_children_depth_wf` — revealing the wrong one silently fails to instantiate. Call site: scoped `assert(uppertree_seq.len()==depth) by { reveal(container_perms_wf); reveal(container_tree_fields_wf); }` + the `container_depth==depth` bridge assert + the lemma call. Lesson: when a deferred lemma "seems to need a big case split," first check its requires are complete — a missing frame/dom hypothesis masquerades as proof difficulty.

**process_thread_wf is now PROVEN** (Xiangdong approved the spec change). Two ingredients: (a) added `old(self).dom().contains(addr) == false` to `LinkedList::push_tail`'s ensures (spec_impl.rs) — surfaces node_perm_disjoint's freshness, monotonic, broke no caller; (b) added a dom-membership conjunct `owned_threads.map().dom().contains(proc_linkedlist_node.addr())` to process_thread_wf's forward clause (process_thread_spec.rs). Then the discharge: forward clause via the fresh-addr + dom-membership (na_t2 ∈ old.dom() ⟹ na_t2 != node_addr ⟹ insert doesn't clobber); reverse clause's `view().contains(page_ptr)` via `seq_push_lemma::<RwLockThreadPtr>()` (THIS is where seq_push_lemma belongs — the reverse contains, NOT the endpoint conjunct). The full-crate spec-strengthening was free (lock ops keep owned_threads byte-equal).

**Scheduler push wired AND its conjunct proven (2026-07-20).** `create_thread_from_staged_page` now takes `scheduler_ptr` + `scheduler_lock_perm`, pushes the fresh thread's `scheduler_linkedlist_node` onto the container's scheduler queue, and flips `state = SCHEDULED` — the fresh thread enters scheduled, per the design fix after `thread_cpu_wf` exposed the RUNNING-without-dispatch bug. The `container_thread_scheduler_wf` re-proof initially looked like a solver-budget wall (hand case-split foralls with all ingredients individually passing but the outer forall failing at 1.5-3.3M rlimit). The /discharge-assume run revealed the truth: the hand foralls WERE the problem (context bloat), and the whole conjunct closes from just `reveal(container_thread_scheduler_wf); reveal(container_thread_wf); reveal(container_scheduler_wf); pre_push_queue.lemma_map_dom(); seq_push_lemma::<RwLockThreadPtr>();` — 3 reveals + 2 lemma calls, NO foralls, NO old-invariant asserts, NO bridges (all delete-and-reverify dead). Same lesson as wlock_allocator_cache's 23-line collapse: when a queue/list grows, `lemma_map_dom` (dom↔addr_list) + `seq_push_lemma` (push contains) are the ONLY quantifier fuel needed; the reveals do the rest. Exec-side survivors: `reveal(scheduler_perms_wf)` before the scheduler borrow_mut, `reveal(container_thread_scheduler_wf)` after it (fresh-thread not-in-queue for push_tail's no-dup precondition), the `pre_push_queue` ghost (read by lemma_map_dom).

Crate at 471 verified, 0 errors. `create_thread_from_staged_page` has ZERO assumes — full inv() re-establishment proven including the scheduler wiring. See [[project_thread_create_skeleton]] and [[project_syscall_new_thread]].
