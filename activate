if [ "$BASH_VERSION" ]; then
  CURRENT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
else
  echo "Unknown shell; exiting."
  return 1
fi

cd "$CURRENT_DIR/verus/source"
source "$CURRENT_DIR/verus/tools/activate"
cd "$CURRENT_DIR"