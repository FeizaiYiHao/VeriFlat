[package]
name = "veriflat_kernel_core"
version = "0.1.0"
edition = "2024"

[lib]
path = "../../src/veriflat_kernel_core.rs"

[dependencies]
vstd = { path = "../../verus/source/vstd" }

[features]
default = ["split-crates"]
split-crates = []

[package.metadata.verus]
verify = true
